{
  "name": "thesuitcase-cuie-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "fs-extra": "^0.30.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src --out-dir dist -w"
  },
  "author": "",
  "license": "ISC"
}
